#include <stdio.h>

int main() {
    // Define Integer variables (size 4 Bytes)
    int franzjosef = 42;
    int hans;
    hans = 1337;
  
    int doerte = franzjosef + hans;
  
    // %d returns Integer value
    printf("%d", doerte);
  
    return 0;
}
