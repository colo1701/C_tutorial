#include <stdio.h>

int main() {
  // Integer definieren (Größe 4 Byte)
  int franzjosef = 42;
  int hans;
  hans = 1337;
  
  int doerte = franzjosef + hans;
  
  printf("%d", doerte);
  
  return 0;
}
